const ejercicio_3 = (name: string): void => {
  /**
   * El ejericio recibe un nombre como parametro y los muestra por pantalla con un mensaje
   *
   * Args:
   *  nombre (String): Nombre a mostrar por pantalla.
   */

  console.log(`Hola, ${name}!`);
};
