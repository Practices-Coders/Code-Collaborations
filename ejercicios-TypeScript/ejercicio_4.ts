const ejercicio_4 = (n_horas: number, c_hora: number): void => {
  /**
   * La funcion recibe como parametro el numero de horas trabajadas
   * y el coste de cada hora y retorna el valor total a pagar.
   * Args:
   *  n_horas (number): Numero de horas trabajadas
   *  c_hora (number): Valor de la hora
   */

  console.log(`AL usuario se le debe pagar $${n_horas * c_hora}`);
};
